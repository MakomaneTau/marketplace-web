# Server-only upstream used by the Next.js API gateway. Never expose API secrets here.
MARKETPLACE_API_URL=http://127.0.0.1:4000

# Deprecated fallback retained during migration. Browser requests use /api/marketplace.
NEXT_PUBLIC_API_URL=http://127.0.0.1:4000
